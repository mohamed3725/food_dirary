/// Toggle Firebase usage for the app.
/// Set `useFirestore = true` after you add Firebase config files
/// (google-services.json / GoogleService-Info.plist) and configure Firebase.
class FirebaseConfig {
  static const bool useFirestore = true;
}
